diff --git a/node_modules/gatsby-plugin-sharp/index.js b/node_modules/gatsby-plugin-sharp/index.js
index 10541fa..2398a7c 100644
--- a/node_modules/gatsby-plugin-sharp/index.js
+++ b/node_modules/gatsby-plugin-sharp/index.js
@@ -12,6 +12,15 @@ var _asyncToGenerator2 = require("babel-runtime/helpers/asyncToGenerator");
 
 var _asyncToGenerator3 = _interopRequireDefault(_asyncToGenerator2);
 
+var resizeWithCropFocus = function resizeWithCropFocus(pipeline, width, height, cropFocus) {
+  var resizeOptions = { fit: sharp.fit.cover };
+  if (typeof cropFocus !== "undefined" && cropFocus !== null) {
+    resizeOptions.position = cropFocus;
+  }
+
+  return pipeline.resize(width, height, resizeOptions);
+};
+
 var notMemoizedbase64 = function () {
   var _ref4 = (0, _asyncToGenerator3.default)( /*#__PURE__*/_regenerator2.default.mark(function _callee2(_ref3) {
     var file = _ref3.file,
@@ -42,7 +51,7 @@ var notMemoizedbase64 = function () {
 
           case 10:
 
-            pipeline.resize(options.width, options.height).crop(options.cropFocus).png({
+            resizeWithCropFocus(pipeline, options.width, options.height, options.cropFocus).png({
               compressionLevel: options.pngCompressionLevel,
               adaptiveFiltering: false,
               force: args.toFormat === `png`
@@ -428,7 +437,7 @@ var notMemoizedtraceSVG = function () {
 
           case 15:
 
-            pipeline.resize(options.width, options.height).crop(options.cropFocus).png({
+            resizeWithCropFocus(pipeline, options.width, options.height, options.cropFocus).png({
               compressionLevel: options.pngCompressionLevel,
               adaptiveFiltering: false,
               force: args.toFormat === `png`
@@ -650,7 +659,7 @@ var processFile = function processFile(file, jobs, cb, reporter) {
               }
               roundedWidth = Math.round(args.width);
 
-              clonedPipeline.resize(roundedWidth, roundedHeight).crop(args.cropFocus).png({
+              resizeWithCropFocus(clonedPipeline, roundedWidth, roundedHeight, args.cropFocus).png({
                 compressionLevel: args.pngCompressionLevel,
                 adaptiveFiltering: false,
                 force: args.toFormat === `png`
